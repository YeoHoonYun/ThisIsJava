New Thread => Runnable -> Blocked => Dead
        start()        suspend()
                       sleep()
                       wait()

yield() 상태는 다른 쓰레드에게 넘겨운
join 해당 쓰레드가 종료될때까지 기다리는 메서드\

데몬
리눅스, 유닉스 게열의 운영체제에서 백그라운드로 동작하는 프로그램

데몬 쓰레드(일반 쓰레드가 모두 종료되면 종료되는 특징)
자바에서 데몬과 유사하게 동작하는 쓰레드